%li
	%span.content
		=wrap(micropost.content)
	%span.timestamp
		Posted
		=time_ago_in_words(micropost.created_at)
		ago.
	-if current_user?(micropost.user)
		=render "shared/delete", :object => micropost